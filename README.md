# newleafsystem monorepo

- `web/` — React web app (Picks site, Invest app, Workbench)
- `api/` — LLM Router and API endpoints (Express, TypeScript)
- `pipeline/` — Yahoo OI service and cron schedulers
- `generaterecommendations/` — Pick generation and tile creation

Each subfolder was previously its own repository. Consolidated on
2026-05-21. No git history preserved from source repos; this is a
fresh start.

Source directories at /Users/manish/dev/* are preserved as fallback.
Do not delete until the monorepo is verified working.
